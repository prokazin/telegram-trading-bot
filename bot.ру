import logging
from aiogram import Bot, Dispatcher, executor, types
from aiogram.contrib.fsm_storage.memory import MemoryStorage
from dotenv import load_dotenv
import os
import random
import asyncio

# Настройки
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)

# Загрузка токена из .env
load_dotenv()
API_TOKEN = os.getenv("BOT_TOKEN")

if not API_TOKEN:
    logger.error("❌ Токен бота не найден! Проверьте файл .env")
    exit(1)

bot = Bot(token=API_TOKEN)
storage = MemoryStorage()
dp = Dispatcher(bot, storage=storage)

# Данные игроков и бизнесов
users = {}
businesses = {}
orders = {}
market = {"BTC": 50000, "ETH": 3000, "AAPL": 150}
active_crisis = None

# Кризисы
CRISES = {
    "economic_crisis": {
        "name": "💸 Экономический кризис",
        "effect": lambda biz: 0.7,
        "message": "Все бизнесы -30% дохода!"
    },
    "cyber_attack": {
        "name": "💻 Кибератака",
        "effect": lambda biz: 0.5 if biz["type"] == "it" else 1,
        "message": "IT-компании -50% дохода!"
    }
}

# Команда /start
@dp.message_handler(commands=['start'])
async def cmd_start(message: types.Message):
    try:
        user_id = message.from_user.id
        if user_id not in users:
            users[user_id] = {
                "usd": 10000,
                "portfolio": {"BTC": 0, "ETH": 0, "AAPL": 0},
                "businesses": []
            }
        await message.answer("Добро пожаловать в игру! Используйте /help.")
    except Exception as e:
        logger.error(f"Ошибка в cmd_start: {e}")

# Пассивный доход от бизнесов
async def passive_income():
    while True:
        try:
            await asyncio.sleep(3600)
            for biz_id, biz in list(businesses.items()):
                user_id = biz["user_id"]
                if user_id in users:
                    income = 100 * biz.get("level", 1)
                    users[user_id]["usd"] += income
                    try:
                        await bot.send_message(
                            user_id,
                            f"💵 Ваш бизнес {biz.get('type', '')} принес ${income}"
                        )
                    except Exception as e:
                        logger.error(f"Не удалось отправить сообщение пользователю {user_id}: {e}")
        except Exception as e:
            logger.error(f"Ошибка в passive_income: {e}")

# Планировщик кризисов
async def crisis_scheduler():
    global active_crisis
    while True:
        try:
            await asyncio.sleep(7200)
            crisis = random.choice(list(CRISES.values()))
            active_crisis = crisis
            
            # Уведомление всех пользователей о кризисе
            for user_id in users:
                try:
                    await bot.send_message(
                        user_id,
                        f"🚨 {crisis['name']}\n{crisis['message']}"
                    )
                except Exception as e:
                    logger.error(f"Не удалось уведомить пользователя {user_id} о кризисе: {e}")
            
            await asyncio.sleep(1800)
            active_crisis = None
            
            # Уведомление об окончании кризиса
            for user_id in users:
                try:
                    await bot.send_message(
                        user_id,
                        "✅ Кризис закончился! Доходы восстановлены."
                    )
                except Exception as e:
                    logger.error(f"Не удалось уведомить пользователя {user_id}: {e}")
                    
        except Exception as e:
            logger.error(f"Ошибка в crisis_scheduler: {e}")

if __name__ == '__main__':
    try:
        loop = asyncio.get_event_loop()
        loop.create_task(passive_income())
        loop.create_task(crisis_scheduler())
        executor.start_polling(dp, skip_updates=True)
    except Exception as e:
        logger.error(f"Фатальная ошибка: {e}")
